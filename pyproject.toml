[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
# PROJ and the headless backend must be set before imports. (import 전에 PROJ와 화면 없는 백엔드를 설정해야 합니다.)
"src/*.py" = ["E402"]
